This is my VIM configs

Installation
============

Clone the repo:
`git clone https://github.com/Zielun/vimfiles.git ~/.vim`

Grab the plugin submodules:
`cd ~/.vim && git submodule init && git submodule update`

Move .vimrc to home
`ln -s ~/.vim/vimrc ~/.vimrc`

